<html lang="en">
    <style>
        body{
   text-align: center;
   background-color: aqua;
}
     </style>
    <body>
        <div>
            <form action="/authors/<%= locals.theId %>?_method=DELETE" method="POST">
                <label>Delete?</br>
                    <input type="text" id="<%= locals.theId %>" name="authors" value="<%= locals.fn %> <%= locals.ln %>" readonly>
                </label>
                <button>DELETE</button>
            </form>
        </div>
    </body>
</html>